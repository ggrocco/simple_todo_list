<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "TODO list" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
	<header>
		<%= render :partial => "shared/user_box" %>
	</header>
    <div id="container">
      <%= yield %>
    </div>
  </body>
</html>
